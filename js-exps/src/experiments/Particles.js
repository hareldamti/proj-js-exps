import { useState } from "react";

var defaultConfig = {
}

var Particles = {
    name: "Particles",
    config: defaultConfig,
    Compoment: () => {
        
        return ( 
            <h1>hi</h1>
         );
    }
}

 
export default Particles;